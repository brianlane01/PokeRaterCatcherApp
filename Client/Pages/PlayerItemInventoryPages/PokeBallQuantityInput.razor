<!-- PokeBallQuantityInput.razor -->

<InputNumber class="form-control" @bind-Value="quantity" min="0" max="@maxQuantity" />
<ValidationMessage For="@(() => quantity)" />

@code {
    [Parameter]
    public int maxQuantity { get; set; }

    [Parameter]
    public int quantity { get; set; }
}
